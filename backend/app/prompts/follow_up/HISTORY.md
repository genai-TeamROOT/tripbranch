# 후속 질문 제안 Prompt History

## 현재 활성 슬롯

| 슬롯 | 관리 버전 | 템플릿 | 공유 규칙 |
| --- | --- | --- | --- |
| follow_up.suggest | 1.2.0 | suggest_instruction.md, capability_rules.md | persona |

## 승인 이력

| 기준선 | 날짜 | 커밋 | 슬롯 | 변경 내용 | 변경 이유 | 상태 |
| --- | --- | --- | --- | --- | --- | --- |
| 1.0.0 | 2026-08-27 | `1d3142e` | `follow_up.suggest` | 한 턴이 끝난 뒤 다음 발화 후보를 만드는 슬롯 신설. 서비스가 실제로 처리할 수 있는 요청 목록(`capability_rules.md`)을 함께 싣는다 | 답변 뒤에 이어서 물을 만한 질문을 버튼으로 제안하는 기능 도입(D-102) | 승인됨 |
| 1.1.0 | 2026-08-27 | (이 커밋) | `follow_up.suggest`(suggest_instruction.md) | 물음표를 실제로 묻는 문장에만 붙이게 하고, 묻는 문장·시키는 문장 예시를 각각 든다. 기존 지침은 "1인칭 요청문" 한 줄뿐이라 요청문에도 물음표가 붙었다("이 근처 카페도 추천해줘?") | 실사용 지적 — 후속 "질문"이라고 전부 의문문은 아니다. 버튼 문구가 그대로 사용자 발화가 되므로 사람이 실제로 칠 문장 모양이어야 한다 | 승인됨 |
| 1.2.0 | 2026-08-27 | (이 커밋) | `follow_up.suggest`(capability_rules.md, suggest_instruction.md) | 기능 목록을 `QuestionType` 17종과 대조해 다시 쓰고, 제안 범위를 "갈 만한 장소를 찾아주는 것"으로 좁힌다. (1) 빠져 있던 편의시설·접근성, 위치·찾아가는 법, 주변 주차장, 공영·시영 주차장, 실시간 행사를 넣는다. (2) "대중교통 시간표 조회"를 할 수 없는 일로 적어둔 오류를 고친다. (3) 지하철 도착·도로 정체·버스정류장·상권 활동과 "거기 어떻게 가?"류 경로 질문은 구현돼 있지만 제안하지 않는 것으로 분류한다 — 소요 시간("얼마나 걸려?")은 갈지 말지를 정하는 재료라 남긴다. (4) 사진 검색은 채팅 문구로 도달할 수 없다는 사실을 명시한다. (5) 내보내기 전에 맞춤법을 검사하게 한다(ㄷ 불규칙 "걷어서"→"걸어서", 조사, 장소명 표기). (6) 문구를 사람이 실제로 칠 법한 한 문장으로 쓰게 하고(길이 상한 30 → 40자), 장소가 정해진 턴에는 주차 질문을 하나 이상 넣게 한다 — 이동수단(`transport`)을 입력에 추가해 `walk`·`public`이면 뺀다 | 목록이 8개뿐이라 되는 기능을 권하지 못했고, 틀린 항목은 되는 기능을 오히려 막고 있었다. 범위 쪽은 소유자 판단이다 — TripBranch는 장소 추천이 목적이지 가는 길을 안내하거나 상권을 분석하는 서비스가 아니다. 30자 상한은 문구를 전보문으로 만들었고("운영시간 알려줘"), 누르면 그게 그대로 사용자 발화가 되므로 사람이 칠 만한 문장이어야 한다. 주차는 출발 전에 거의 항상 걸리는 문제라 자리를 하나 고정하되, 걷거나 대중교통으로 움직이겠다고 말한 사용자에게는 버튼 하나를 버리는 셈이라 조건을 함께 넘긴다 | 승인됨 |

인텐트 폴더가 아닙니다 — 인텐트 하나에 속하지 않고 어떤 인텐트로 끝난 턴이든 그 뒤에 한 번
도는 슬롯이라 `meta.yaml`에 `intent` 키가 없습니다(`synthetic_review/`와 같은 형태).

`capability_rules.md`가 이 슬롯의 핵심입니다. 모델은 여기 적힌 목록만 보고 무엇을 제안할 수
있는지 판단하므로, **서비스에 기능이 늘거나 빠지면 이 파일을 함께 고쳐야 합니다.** 틀리는
방향이 둘이라 양쪽 다 봅니다 — 없는 기능을 적으면 누른 사용자가 OUT_OF_SCOPE 답변을 받고,
있는 기능을 빠뜨리면 그 기능은 영영 제안되지 않습니다. 1.2.0이 후자를 고친 변경입니다.

목록이 세 갈래인 이유가 있습니다 — **"할 수 없는 일"과 "있지만 제안하지 않는 것"은 다릅니다.**
전자는 요청해도 안 되는 것이고, 후자는 사용자가 직접 물으면 INFO가 답하지만 우리가 먼저
권하지는 않기로 한 것입니다(지하철 도착, 도로 정체, 버스정류장, 상권 활동, 사진 검색). 후자를 "할 수 없는 일"에
적으면 파일이 거짓말을 하게 되고, 나중에 읽는 사람이 구현된 기능을 없는 것으로 오해합니다.

**목록의 기준은 "채팅 문구로 도달하는가"입니다.** 버튼은 문구를 그대로 `/api/chat`에 보내는
것이 전부라, 기능이 있어도 다른 진입점을 거쳐야 하면 넣으면 안 됩니다. 사진으로 닮은 장소를
찾는 기능(`POST /api/places/similar-by-photo`)이 그런 경우라 파일에 이유와 함께 적어두었습니다.

INFO 쪽 목록은 `app/schemas.py`의 `QuestionType`과 `info/question_type_rules.md`가 기준입니다.
질문 유형이 늘면 여기도 함께 늘려야 합니다.

길이 상한(`MAX_LABEL_LENGTH`)은 짧게 만들라는 뜻이 아니라 화면에 실리는 한계입니다. 30자였을
때 문구가 전보문처럼 나와 1.2.0에서 40자로 올렸습니다 — 버튼에 들어가는 것보다 **사람이 실제로
칠 만한 문장**인 쪽이 중요합니다.

맞춤법도 프롬프트에만 맡기지 않습니다. 다만 `follow_up_suggester.py`의 `_TYPO_FIXES`는
**일반 맞춤법 검사기가 아니라 실제로 관측된 오류 목록**입니다 — 한국어 맞춤법을 코드로
일반화하려면 형태소 분석기나 외부 검사 API가 필요한데 버튼 문구 하나 때문에 의존성을 늘릴
자리가 아닙니다. 새 오류가 눈에 띄면 그때 한 줄 추가하고, 그 밖은 이 슬롯의 지침이 맡습니다.

물음표 규칙은 명령형 어미(`-줘` 등)에 한해 `follow_up_suggester.py`가 한 번 더 걷어냅니다.
무엇이 질문인지를 어미만으로 가르는 일반 규칙은 만들 수 없어("얼마나 걸려?"는 물음표가
있어야 맞습니다) 의문문이 될 수 없는 어미만 목록으로 두었고, 나머지는 이 슬롯의 지침이
맡습니다.

개수·길이·중복·직전 발화 반복은 프롬프트에도 적혀 있지만 실제 계약은
[`follow_up_suggester.py`](../../services/runtime/follow_up_suggester.py)의 코드 검사입니다.
이 슬롯의 문구를 손볼 때 상한을 바꾸려면 그쪽 상수(`MAX_SUGGESTIONS`, `MAX_LABEL_LENGTH`)를
함께 봅니다 — 지침에만 적으면 화면에 반영되지 않습니다.

## 1.1.0 확인 방법

2026-08-27 로컬 개발 서버(`:8000`)에서 실제 Gemini 호출로 수동 확인했습니다 — 요청문에
물음표가 붙는 빈도가 눈에 띄게 줄었습니다.

**표본을 수치로 남기지 못했습니다.** 이 저장소는 프롬프트 버전을 올릴 때 before/after를
표로 남기는 것을 관례로 하는데, 여기서는 케이스를 정해 돌린 것이 아니라 화면에서 확인한
결과입니다. 다음에 이 슬롯을 손볼 때는 `evals/`에 케이스를 먼저 만들고 시작하는 편이
좋습니다 — 그때 이 1.1.0이 기준선 역할을 할 수 있게.

## 실행 가능한 과거 기준선

- [suggest_instruction__legacy-1.0.0.md](archive/suggest_instruction__legacy-1.0.0.md):
  물음표 규칙이 들어가기 전의 원문입니다. 함께 쓰는 `capability_rules.md`와
  `_shared/persona/trivi.md`는 이 변경으로 바뀌지 않아 `variants.json` 없이 이 파일만
  되돌리면 재현됩니다.
- [suggest_instruction__legacy-1.1.0.md](archive/suggest_instruction__legacy-1.1.0.md) ·
  [capability_rules__legacy-1.1.0.md](archive/capability_rules__legacy-1.1.0.md): 기능 목록을
  다시 쓰기 전의 원문입니다. 1.2.0은 두 파일을 함께 고쳤으므로 되돌릴 때도 둘을 함께
  되돌립니다.
